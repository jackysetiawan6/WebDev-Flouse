let popupDetail = document.getElementById("popupDetail");
let popupNotes = document.getElementById("popupNotes");

function toggleDetailPopup() {
    popupDetail.classList.toggle("active");
}

function toggleNotesPopup() {
    popupNotes.classList.toggle("active");
}